name: CI/CD Tienda Perritos - Backend

on:
  push:
    branches: [ "main" ]
    paths:
      - "backend/**"

jobs:
  build-push-deploy-backend:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v3

    - name: Configurar credenciales AWS
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login en Amazon ECR (runner)
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build imagen Backend y push a ECR
      run: |
        cd backend
        docker build -t tienda-backend .
        docker tag tienda-backend:latest ${{ secrets.ECR_REPO_URL_BACKEND }}:latest
        docker push ${{ secrets.ECR_REPO_URL_BACKEND }}:latest

<<<<<<< HEAD
    # Removed redundant Tag/Push steps — image is already tagged and pushed above using secrets.ECR_REPO_URL_BACKEND
=======
    - name: Tag image
      run: |
        docker tag $ECR_REPOSITORY:latest ${{ steps.login-ecr.outputs.registry }} $ECR_REPOSITORY:latest

    - name: Push image
      run: |
        docker push ${{ steps.login-ecr.outputs.registry }} $ECR_REPOSITORY:latest
>>>>>>> c45509b6ef34b918462a690348e9125b54350389
